name = "portfolio-demos"
main = "worker.js"
compatibility_date = "2026-08-01"

[vars]
# Not secret — just identifiers, safe to keep in plain text
ROBOFLOW_MODEL_SKETCHFUL = "sketchful.ai-elpny/1"
ROBOFLOW_MODEL_XRAY = "outsident/1"
HF_MODEL_AGE = "crangana/trained-age"
HF_MODEL_GENDER = "crangana/trained-gender"
HF_MODEL_RACE = "crangana/trained-race"

# The following are set as encrypted secrets, NOT here — see DEPLOY.md:
#   ROBOFLOW_API_KEY
#   HF_TOKEN_AGE
#   HF_TOKEN_GENDER
#   HF_TOKEN_RACE
